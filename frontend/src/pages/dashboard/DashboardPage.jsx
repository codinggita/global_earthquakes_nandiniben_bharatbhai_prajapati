// DashboardPage — Batch 3 will build this fully
// Placeholder stub so routes don't break
const DashboardPage = () => (
  <div style={{ padding: '2rem', color: 'var(--color-text-secondary)', textAlign: 'center' }}>
    <h2 style={{ color: 'var(--color-text-primary)', marginBottom: '0.5rem' }}>
      🚧 Dashboard
    </h2>
    <p>Coming in next batch — earthquake stats, charts, and live data.</p>
  </div>
);

export default DashboardPage;
