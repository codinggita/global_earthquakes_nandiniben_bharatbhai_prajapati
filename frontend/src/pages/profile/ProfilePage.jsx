// ProfilePage — Batch 6 will build this fully
const ProfilePage = () => (
  <div style={{ padding: '2rem', color: 'var(--color-text-secondary)', textAlign: 'center' }}>
    <h2 style={{ color: 'var(--color-text-primary)', marginBottom: '0.5rem' }}>
      👤 Profile
    </h2>
    <p>Coming in next batch — user info, edit form, and theme settings.</p>
  </div>
);

export default ProfilePage;
